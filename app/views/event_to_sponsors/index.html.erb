<h1>Listing event_to_sponsors</h1>

<table>
  <tr>
  </tr>

<% @event_to_sponsors.each do |event_to_sponsor| %>
  <tr>
    <td><%= link_to 'Show', event_to_sponsor %></td>
    <td><%= link_to 'Edit', edit_event_to_sponsor_path(event_to_sponsor) %></td>
    <td><%= link_to 'Destroy', event_to_sponsor, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New event_to_sponsor', new_event_to_sponsor_path %>